+ Added script syntax in layout
+ Added "Settings" application
+ Added "wizard" command

* Seperated the translators section from the rest of the "Credits" app
* Moved the "Specifications" application back to the "Random" tab
* If a dialog entry is too large, it will use a smaller font
* Now you can use the application's raw name (without translation) as an argument for the "open" command
* Added a pause in the wizard between when the "Hello" appears and the "Click to continue ..."

- Drastically reduced RAM consumption and file size
